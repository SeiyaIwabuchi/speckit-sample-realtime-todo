import { describe, it, expect } from 'vitest';

describe('Todoからタグ削除', () => {
  it('Todoから個別にタグを外せる', () => {
    // 未実装: UI操作でタグを外し、Todo詳細から消えること
    expect(false).toBe(true); // TDD: 失敗すること
  });
});
