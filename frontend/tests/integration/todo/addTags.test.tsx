import { describe, it, expect } from 'vitest';

describe('Todoへのタグ追加', () => {
  it('Todoに複数タグを追加できる', () => {
    // 未実装: UI操作でタグを追加し、Todo詳細に反映されること
    expect(false).toBe(true); // TDD: 失敗すること
  });
});
