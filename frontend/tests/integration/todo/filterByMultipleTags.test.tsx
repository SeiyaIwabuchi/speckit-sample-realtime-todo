import { describe, it, expect } from 'vitest';

describe.skip('複数タグフィルタ（OR条件）', () => {
  it('複数タグを選択してTodoをフィルタリングできる（OR条件）', () => {
    // 未実装: 複数のタグを選択し、いずれかのタグが付いたTodoが表示されること
    expect(false).toBe(true); // TDD: 失敗すること
  });
});